<%= form_for(@job_group) do |f| %>
  <% if @job_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@job_group.errors.count, "error") %> prohibited this job_group from being saved:</h2>

      <ul>
      <% @job_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :hour_rate %><br>
    <%= f.number_field :hour_rate %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
